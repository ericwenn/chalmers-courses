version: '3.2'
services:
  api:
    container_name: api
    image: api
    build: api/.
    ports:
      - 8901:3000
    environment:
      - MONGO_URL=mongodb://mongo/ccourses

  client:
    build: 
      context: client/.
      args:
        - REACT_APP_GRAPHL_URL=http://167.99.17.131:8901

    ports:
      - 8902:80

  mongo:
    image: mongo
